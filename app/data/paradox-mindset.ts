import { questionGroups } from './generated/paradox-mindset';

export const scoreOptions = [1, 2, 3, 4, 5, 6, 7];

export const questionsTension = questionGroups[0] ?? [];
export const questionsParadox = questionGroups[1] ?? [];
